#include <stdio.h>
/**
 * main - return the follqdfqlmsfd
 *
 * Return: always 0(SUCCESS)
*/
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
