#include "main.h"
/**
 * _strlen - returns the length of an str using fseek
 * @s: the perimiter to check
 * Return: the size of s
*/

int _strlen(char *s)
{
	char *p = s;
	int i = 0;

	while (p)
	{
		i++;
	}
	return (i);
}
